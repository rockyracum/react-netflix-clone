import { LoadingIcon } from "../../assets/icons";
import { Container } from "./styled";

function Loading() {
    return (
        <Container>
            <LoadingIcon />
        </Container>
    );
}

export default Loading;
